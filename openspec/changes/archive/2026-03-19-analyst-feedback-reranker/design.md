# Design

SQLite-backed feedback records were keyed to rows/pattern tags. Monitor responses carried review metadata, while the optional reranker consumed reviewed examples without replacing candidate generation.
