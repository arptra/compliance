# Design

Preparation metadata carried service-column selection into monitor presets. Upload runs normalized duplicates, detected likely datetime/dialog columns, and selected mock LLM only when mTLS prerequisites were absent.
