# Design

The UI cleared stale run paths before execution, gated concurrent starts, refetched only after run completion, and rendered the selected run artifact. Backend endpoints returned explicit errors and robust empty results.
