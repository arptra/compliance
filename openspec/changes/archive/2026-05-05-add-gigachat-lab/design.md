# Design

FastAPI exposed transport, settings, workbook, prompt, and row-run endpoints. React composed focused Lab components around a large orchestration page. Local/VM scripts resolved hosts and ports consistently.
