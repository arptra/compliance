# Design

The server parsed/exported a stable workbook schema and persisted rule packs inside settings versions. Matching operated only on available fields and retained significant keyword whitespace.
