# Design

Upload-specific config overrides produced preparation jobs and monitor presets. The monitor resolved tagged output paths, invalidated stale caches, and exposed run progress/errors through API state.
