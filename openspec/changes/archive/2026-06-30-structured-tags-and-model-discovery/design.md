# Design

Response normalization converged tag shapes into a stable DTO. Probe responses carried model ids to the frontend selector. Exporters resolved annotation-name collisions rather than overwriting source data.
