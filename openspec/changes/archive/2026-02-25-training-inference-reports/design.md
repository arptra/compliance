# Design

The pipeline normalized object columns before Arrow conversion, used the configured dialog fallback, scoped validation plots to the intended time window, and constrained model outputs to taxonomy values.
