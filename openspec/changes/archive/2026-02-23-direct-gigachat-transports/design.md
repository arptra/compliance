# Design

The implementation centered on `gigachat_mtls.py`, direct `httpx` requests, shared TLS context construction, and Pydantic normalization of model output. Later token transport work extended this boundary.
